package com.example.tour.domain;

import org.springframework.data.repository.CrudRepository;

public interface OverseasRepository extends CrudRepository<Overseas, Long>{    // Repository Pattern Interface
  
}